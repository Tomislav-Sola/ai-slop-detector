# pr-triage reads GITHUB_TOKEN and ANTHROPIC_API_KEY from the shell environment.
# Shell variables always take priority; this file is a fallback for variables not
# already set in your session.  Copy to .env for local development — never commit .env.
GITHUB_TOKEN=ghp_your_token_here
ANTHROPIC_API_KEY=sk-ant-your_key_here
